/fable Sotto overnight run 2: make the hosted link the full product. Sotto is an open-source, free, local-first graded-reader app already live at https://sotto-steel.vercel.app (reading only, no tutor). Tonight: the voice tutor moves into the browser with no server, the first visit lands in two taps on phone and desktop, the site installs and works offline, the reader gains phrase and sentence translation, narration sync gets accurate for FR/ES, explanation glosses exist for all nine locales, and the verification debt is paid with evidence. All planning is done; execute as orchestrator and finish with proof.

Read, in this order, before doing anything else:
1. ~/Claude/sotto/planning/OVERNIGHT-2.md (the plan: current state, five lanes with file ownership, gates, proof, routing, deferrals)
2. ~/Claude/sotto/planning/LEDGER.md (append; every dispatch gets a seven-field task card first)
3. ~/Claude/sotto/docs/verification.md and ~/Claude/sotto/planning/ADVERSARIAL-REVIEW.md (what is real today and how the last review found gaps)
4. ~/Claude/sotto/planning/design/DESIGN.md and ~/Claude/sotto/README.md
5. ~/Claude/sotto/planning/DECISIONS.md and CONTRACTS.md only when a lane needs a rule from them

Operating rules:
- You are the orchestrator. You plan, dispatch, review, integrate, verify. Subagents and /fanout workers write the code. Direct edits only for glue and fixups. Fable turns: pre-flight, Gate 1, Gate 2, close-out.
- Routing per OVERNIGHT-2.md: Opus for Lane B design plus first slice and for the adversarial review; Sonnet for every other implementer (omit the model parameter or say sonnet/opus, never haiku); /fanout for content volume. Each worker gets exact files, exact interfaces, done-criteria, and its ownership ceiling. Workers do not grade their own work; you check against the plan's proof list in the Browser pane and on disk.
- Pre-flight: run pnpm check on the current tree. If green, commit the 17 uncommitted files as "wip: deploy-session follow-ups (serverUrl origin fallback, voice error codes)". If not green, leave them uncommitted, record why in the ledger, and keep lanes off those files. Then open -a Docker, ~/ods/installers/macos/ods-macos.sh start, whisper-server on :9001 (command in README), llama-server with cache_prompt true and enable_thinking false, and confirm GET http://127.0.0.1:8790/health reports stt, llm, tts all true before dispatching lanes C and D.
- Waves and gates exactly as OVERNIGHT-2.md: wave 1 = A, B, C1+C2, D1+D2 in parallel with disjoint ownership; Gate 1 review; wave 2 = A5 deploy and hosted smoke, B remaining slices, C3, D3, E; Gate 2; adversarial review; fix lane; close-out.
- Deploy only from apps/client with the documented CLI path (pnpm web:export, vercel build --prod --yes --scope nturls-projects, vercel deploy --prebuilt --prod --yes --scope nturls-projects). Add pnpm deploy:web wrapping it. After every deploy run the hosted smoke against the live URL and read the console.
- Commit and push to origin main at every gate; stage only files this run changed. Never rewrite history, never stash, never reset.
- Verify before declaring anything done: launch it in the Browser pane at 375 and 1440, read console and network, take screenshots, save evidence logs under docs/evidence/. A worker's "done" is a claim until you have looked.
- In-browser tutor rules: models download only on explicit user opt-in with sizes shown; unsupported browsers keep the clear unavailable panel; no server, no account, no analytics. If WebGPU headless fails twice, follow the plan's fallback and label what ran honestly.
- Content rules: public-domain abridgments only, every new book labeled draft with license metadata, narration Kokoro-generated with whisper-aligned timings, validator green. Generated sentence translations and glosses go through the validator; the level-sanity report never promotes a book.
- Never: commit secrets, add accounts or payments or analytics, name the reference app anywhere, write token allowances into prompts, ask me routine questions. Ask only if the central journey on the hosted link is blocked.
- Cost: keep stable context first in every worker prompt. If the all-models window trips, move waiting content work to /fanout instead of idling.

Finish line = OVERNIGHT-2.md "Proof the orchestrator must hold at close-out", all three layers. Then: adversarial review (Opus, "what is fake, fragile, or unverified"), fix the top findings, update docs/verification.md rows touched tonight with evidence paths, add a README "Try it" section with the link and a short demo GIF, append the finish-line section to LEDGER.md, update ~/.claude/projects/-Users-noelturlington-Claude/memory/project-sotto-reading-app.md, and send me one iMessage: the link, what changed per lane, what was deferred and why, and the changed-file count. Text me also if you are blocked on me.
