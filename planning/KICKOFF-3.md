Sotto run 3: free local tier + paid hosted tier. Sotto is an open-source graded-reader app (repo ~/Claude/sotto, live at https://sotto-steel.vercel.app). This run adds (1) book import on the free, local, open-source tier, (2) a community path for adding public-domain readers by PR, (3) a paid hosted tier with accounts, subscriptions on web and iPhone, an OpenAI-backed voice tutor with per-plan minute caps, hosted processing of uploaded books, and an App Store build. Planning is done; execute as orchestrator and finish with proof. You are on Opus because accounts, keys, and billing make this security-adjacent; do not hand any of that to Fable.

GATE 0 — do nothing else until overnight run 2 has closed out.
Run 2 is a separate session working in the same repo. It is done when ALL of these hold:
(a) ~/Claude/sotto/planning/LEDGER.md contains a heading matching /^## .*(finish line|close-out|closeout)/i that appears AFTER line 181 (the "## O2-C alignment before/after" heading that existed when this prompt was written);
(b) `git -C ~/Claude/sotto status --porcelain` is empty or lists only files under planning/ (this prompt's own PAID-TIER-PLAN.md and KICKOFF-3.md are untracked and expected);
(c) `git -C ~/Claude/sotto rev-parse HEAD` equals `git -C ~/Claude/sotto rev-parse origin/main` after `git fetch origin`.
Arm ONE Monitor (persistent: true) whose script polls every 120 seconds, prints exactly one line "RUN2_DONE <commit>" and exits when (a)-(c) all hold, and prints "RUN2_STALE <hours>" (without exiting) if no commit has landed on origin/main for 3 hours and (a) is still false. Print nothing otherwise. While waiting, do only read-only preparation: read the files listed below, re-verify every file path in PAID-TIER-PLAN.md against the tree, and draft task cards in your own notes without writing to the repo. On RUN2_DONE, read run 2's finish-line section and the updated docs/verification.md before dispatching anything, and adjust the plan for what run 2 actually shipped (in-browser tutor, PWA, phrase translation, dark mode may all exist now). On RUN2_STALE, send Noel one iMessage saying run 2 looks stalled with the last commit time, and keep waiting; do not start run 3 on a dirty tree.

Read, in this order:
1. ~/Claude/sotto/planning/PAID-TIER-PLAN.md (feasibility verdict, open-core shape, five lanes with ownership, gates, the twelve known unknowns with defaults, routing)
2. ~/Claude/sotto/planning/LEDGER.md (append; every dispatch gets a seven-field task card first)
3. ~/Claude/sotto/planning/DECISIONS.md and CONTRACTS.md (CONTRACTS §0 "never add auth, payments, analytics" stays true for the OSS repo; cloud code goes in the new private repo sotto-cloud)
4. ~/Claude/sotto/docs/verification.md, docs/openai.md, docs/architecture.md, docs/voice-pipeline.md, README.md
5. packages/content/src/{build,gloss-fill,translate-sentences,narrate,align}.ts (the pipeline the importer reuses; do not fork it)
6. ~/Claude/sotto/planning/design/DESIGN.md and DESKTOP.md before any new screen

Operating rules:
- You plan, dispatch, review, integrate, verify. Subagents write the code. Direct edits only for glue. Routing per the plan: Opus subagents for accounts, entitlements, the voice broker, webhooks, and the adversarial review; Sonnet for the importer, community path, store/paywall, verification, and fixes (omit the model parameter or say sonnet/opus, never haiku). Each worker gets exact files, exact interfaces, done-criteria, and an ownership ceiling. Workers do not grade their own work.
- Two repos. OSS: ~/Claude/sotto, Apache-2.0, must remain fully useful with no cloud and no account. Cloud: create github.com/nturl/sotto-cloud PRIVATE at ~/Claude/sotto-cloud with `gh repo create --private`; it depends on @sotto/core, @sotto/content, @sotto/voice. The client stays one Expo app with a CloudAdapter interface and a NullCloud default; account and paywall UI render only when a real adapter is present.
- Importer: EPUB (DRM-free; detect DRM and refuse clearly), TXT, Markdown. PDF deferred and labeled. Reuse the content pipeline as a library (importBook with progress events); lazy narration by chapter; private packs stored locally, never under packages/content/packs, never in the repo. Free tier runs it on the local stack with honest time estimates in the UI.
- The other-language layer for an uploaded book is GENERATED (glosses, sentence translations, narration, timings), private to that user, never shared or deduplicated across users, deleted with the account. Say this plainly in docs/importing-books.md and in the upload screen. Do not build anything that fetches or redistributes a translated edition.
- Voice cost is the central risk. Entitlement table is the single source of truth for plan, tutor minute cap, minutes used, import cap; the broker enforces caps server-side, meters by audio seconds, and refuses when exhausted with a clear in-app message. Default provider is the OpenAI cascade (mini-transcribe -> small chat model -> mini-tts); gpt-realtime-mini on the standard plan; full gpt-realtime only as a metered add-on. Add a daily spend ceiling, a kill-switch env var, and a usage page for Noel. Never put a standard API key anywhere but sotto-cloud's server env; the client gets short-lived session secrets only.
- Billing: StoreKit 2 subscriptions on iOS (decide expo-iap vs RevenueCat and record why), Stripe Checkout on web, both feeding the entitlement table by webhook. US storefront may show the web price with a plain link, IAP stays the primary button. Account deletion in-app. Sign in with Apple plus email magic link. No paywall mid-session; free tier is nagged at most once per session.
- Store: EAS Build, TestFlight, privacy manifest, purpose strings, privacy labels, App Review notes with a demo account. If no Apple dev team is configured, do everything up to the upload and hand Noel the exact upload command; that is his step, not a blocker for the rest.
- Placeholders that are Noel's decisions (prices, caps, Realtime vs cascade headline, whether sotto-cloud goes public, legal posture on uploads) are implemented as config with the defaults from the plan's known-unknowns table, and listed under CONFIRM in the first iMessage. Do not wait on them.
- Hosting: sotto-cloud on Fly.io (WebSocket-capable), static client stays on Vercel via the documented CLI path; every deploy is followed by the hosted smoke and a console read.
- Commit and push at every gate in both repos; stage only files this run changed. Never rewrite history, never stash, never reset. Before the first commit in ~/Claude/sotto, also commit planning/PAID-TIER-PLAN.md and planning/KICKOFF-3.md.
- Verify before declaring anything done: Browser pane at 375 and 1440, console and network read, screenshots and logs under docs/evidence/. A worker's "done" is a claim until you have looked. Live Realtime is verified once with a real key and that session's cost goes in the ledger.
- Never: commit secrets, name the reference app, write token allowances into prompts, add analytics or telemetry SDKs, ask me routine questions. Ask only if the central paid journey (sign in -> subscribe -> tutor session under a cap) is blocked on something only I hold.

Finish line = PAID-TIER-PLAN.md lanes I, P, C, S, E with the Gate 2 proofs: importer e2e log (EPUB -> readable private pack with narration on web, local stack); paid e2e log against staging (sign in, subscribe in sandbox/test mode, cascade tutor session, cap reached, clear message); one live Realtime session with its measured cost; TestFlight build uploaded or the exact hand-off command; 375/1440 screenshots of import, paywall, account, and usage screens; `pnpm check` tails for both repos; a boundary check that the OSS repo still has no auth, payments, or analytics and runs with no cloud. Then: adversarial review (Opus, "what leaks, what costs, what is fake"), fix the top findings, add a Tier 4 hosted section to docs/verification.md, write docs/importing-books.md and docs/app-store.md, append the finish-line section to LEDGER.md, update ~/.claude/projects/-Users-noelturlington-Claude/memory/project-sotto-reading-app.md, and send me one iMessage: what shipped per lane, the CONFIRM list with your defaults, what was deferred and why, the measured per-minute voice cost, and the changed-file count per repo. Text me also if you are blocked on me.
