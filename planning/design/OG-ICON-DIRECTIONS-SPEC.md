# Sotto card and tile, directions sheet 1: spec

File: `planning/design/og-icon-directions.html` (one file, fonts embedded, no requests).

**Fit read.** Job: Noel points at one of three, likely on his phone, one evening; so a bench sheet, bands, each showing the link card at the size iMessage renders it and the tile at the sizes a home screen renders it.

**Register.** Directions sheet (bands). Rotation: the last two rows are catalog and poster; the sheet form last appeared three rows back, forced here by the mode.

**Axis.** What a stranger meets first: the object (A, the teal cover), the mechanism (B, a marked word and its gloss), or the voice (C, the promise line in coral). Each band carries both the 1200 x 630 card and the icon, plus what the landing's install book becomes.

**Anchor.** Teenage Engineering industrial catalogue, adapted: specimens on a warm bench `#E7E3DB` instead of grey, spec labels in tracked mono, direction names in Fraunces 300. Specimens keep Sotto's own tokens (`#F4ECDF`, `#1F4F57`, `#E4572E`, `#FFD8A8`, `#F2C8B4`).

**Device.** The delivery-size mock: each card rendered inside an iMessage bubble at 300px and each tile at 180, 60, 29 and inside the Android circle mask. No decoration beyond that.

**Type.** Fraunces 300 embedded for display and the marks; system sans 17px/1.55 for notes; mono 12px tracked for metadata.

**Grid.** 64px page gutters, bands separated by hairlines, stage = card 600px beside the bubble, tiles on a full-width row below, notes in a 5-column grid that collapses to one at 640.

**Ban list.** No mockup phone chrome beyond a flat home-screen strip; no photography; no gradients; no fourth direction; no invented copy on the cards (only lines already on the landing).

**Definition of done.** cleo_verify full run 0 FAIL, every WARN named; four screenshots read; the chips and reply builder work by hand; at 375 the card scales to 336px and nothing overlaps; the B gloss does not collide with the sentence's third line.
