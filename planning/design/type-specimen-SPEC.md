# Sotto type specimen — spec

**Fit read.** Job: inspect the range of the one face Sotto ships (Fraunces 300 and 400) and copy exact values into the app; desktop working doc, opened whenever a type decision comes up; neutral technical temperature. So: light specimen sheet on the app's own canvas.

**Register.** Specimen: one object at full scale (an editable word in Fraunces), a control rail that changes it, a grid of its variants (the DESIGN.md type roles set 1:1, and the eight content locales' real first sentences), spec columns in mono. Clean rotation after directions sheet / poster+mark / directions sheet.

**Anchor.** Hawker Type (kombai-gallery s.3), adapted: cream #F0EAD8 becomes Sotto canvas #F4ECDF; the hero drops from Fraunces 900 to 300; orange #D25A30 becomes Sotto coral #E4572E with one job (the value you are changing: thumb, live readout, active preset); DM Mono becomes system mono; the hexagon glyph grid becomes ruled rows; the blinking caret is cut (F7).

**Flood.** Sotto canvas #F4ECDF, surface #FBF6EC, ink #221E1B, ink-2 #6E6459. Single committed light mode.

**Device.** Variable specimen rail. Honest axes only: the machine has no variable Fraunces, so weight is a two-stop control across the two static instances the app actually loads; size, leading, tracking and measure are continuous. Presets are the real DESIGN.md roles. Ends with a copy-CSS control.

**Type cast.** Display and reading: Fraunces 300/400 embedded (the launch-cards b64). Metadata: system mono 11px tracked .08em uppercase. UI: system sans.

**Grid.** Hero 1fr + 300px rail; rows under it are full-width hairline-ruled bands; 24px gutters at 375, 48px at 1280.

**Ban list.** No fake axes (no opsz, SOFT or WONK sliders). No Chinese in Fraunces (system CJK serif, captioned). No third colour. No cards; rules only.

**Output.** `~/Claude/sotto/planning/design/type-specimen.html`, one file, fonts inline.

**Done when.** cleo_verify full run 0 FAIL, WARNs named; four screenshots read; rail moves the hero, the sentences and the readout; copy control works; ledger row + archive entry.
