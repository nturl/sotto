# Lane A: the landing page, redesigned around the learner

Task: rebuild `apps/client/web/landing/index.html` so a first-time visitor understands what Sotto is, starts free, or signs in, and can find own-provider mode, self-hosting and install guidance without any of them reading as a subscription tier.

Inputs: `planning/KICKOFF-7-FABLE.md` §"Redesign the landing page" and §"Product model"; `planning/run7/PLAN.md` (CONFIRM 22, 23); `~/Claude/sotto-run7-recon/scout-L-landing-account.md` §1-3, §9-10 (claims table is the checklist); `planning/design/LANDING.md`, `LANDING-V2.md`, `LANDING-V3.md`, `DESIGN.md`; the Cleo director persona `~/Claude/Agents/agents/cleo.md` and its knowledge base `~/Claude/Agents/design/` (read `LEDGER.md` top 10 rows + Rotation, `brief-anatomy.md`; run `tools/cleo_verify.py`). Product screenshots to reuse: `apps/client/docs/screenshots/web/` and `docs/screenshots/`. The current live page is byte-identical to the file.

Owned files: `apps/client/web/landing/**`, `planning/design/LANDING-V4.md`, `~/Claude/Agents/design/LEDGER.md` (one new row) and `log-archive.md`.

Directives:
1. Act as Cleo first: write `planning/design/LANDING-V4.md` (skeleton chosen from the rotation, anchor system, one signature move, ban list) BEFORE editing HTML. Keep the warm bookish character: cream surfaces, editorial type, restrained colour. Noel liked "Sotto reads with you." and the passage device; keep the headline, label the passage "A sample passage · Level A2" (or drop the meta line), never leave it unexplained (D-6).
2. Hierarchy, top to bottom: (1) promise in one sentence about reading, listening, talking; (2) primary "Start free" → `https://app.readsotto.app/account?intent=start`, secondary "Sign in" → `https://app.readsotto.app/account`, tertiary "Try a sample" → `/start` on this origin with one honest line: progress and saved words stay in this browser until you create an account; (3) product preview: real screenshots (reader with word popup, tutor) captioned, not stock; (4) read → understand → speak in three short lines; (5) Free vs the plan: free = read, listen, tap for meaning, save words, tutor with browser models or your own key; the plan = the hosted tutor with nothing to set up, $9.99/month or $79/year after a 3-day trial (only these numbers, no minute counts); (6) secondary guidance: "Use your own key" (works with or without the plan, the provider bills you, key stays in this browser) and "Run it yourself" → `docs/self-hosting.md`; (7) Install Sotto: detect iOS Safari / Android Chrome / desktop Chrome via JS and show only that device's steps, with a generic fallback; no offline promise beyond "a book you have opened keeps working offline" (verified); (8) source, docs, support links.
3. Remove "Free · Open source · No account" from the header. Explain "graded reader" as books adapted to your level; keep the machine-adapted-draft and estimated-level caveats but place them next to book choice, not the hero.
4. Privacy sentences must be true per mode (scout-L §2): "everything runs in your browser" only for browser models and own-provider; "nothing is recorded" may stay for the plan (verified: no transcripts stored) but say what the provider receives in own-provider and plan modes.
5. Keep the page static, no framework, under the existing CSS token approach; keyboard focus visible, contrast AA, `prefers-reduced-motion` honoured; body never scrolls horizontally at 375.

Proof: `cleo_verify` 0 FAIL; screenshots at 375 and 1440 in `~/Claude/sotto-run7-recon/A/`; every href resolved with curl (GitHub docs 200, paid origin 200); a table in the report re-checking each privacy/capability sentence against scout-L §2; the install block shown for each of the three detected devices (Playwright UA override). Do not deploy; the orchestrator deploys after review.

Stop when: LANDING-V4.md, the page, the ledger row and `planning/run7/A-report.md` are committed and pushed. Escalate when: a claim cannot be made true without a code change outside your files (write the sentence and the file).
