# Lane C: account creation, sign-in, onboarding (Opus, separate process)

Read `planning/run7/cards/COMMON.md` first. Then this card. Model: Opus. This lane is cordoned because it touches sessions, magic links and the entitlements table; it runs as its own process, not a subagent.

Task: make "Sign in" and "Start free" real: a sign-in/create screen with email magic link (and Apple only where it actually works), clear returning vs new paths, loading/cancel/error states, a visible account area, a sensible return destination, onboarding that never asks for a key and separates the four language/level questions, and an honest guest-to-account story.

Inputs: `planning/run7/PLAN.md` (ground truth 3, 4, 5; CONFIRM 23, 24); `~/Claude/sotto-run7-recon/scout-L-landing-account.md` §4-8 (account surfaces, auth routes, plans, gating, onboarding entry, guest data); `~/Claude/sotto-run7-recon/scout-1-navigation.md` §4 (onboarding as built); `planning/design/ACCOUNT.md`, `PAYWALL.md`, `DESIGN.md`; `~/Claude/sotto-cloud/DECISIONS.md`, `docs/`, `src/auth/routes.ts`, `src/static.ts`, `src/plans.ts`, `src/entitlements.ts`; `planning/KICKOFF-7-FABLE.md` §"Make sign-in and onboarding real". Decision defaults in force: D-1 accounts live on the paid origin; D-2 no Google this run, write it up; D-5 NO Fly deploy.

Owned files: `apps/client/app/account/**`, `apps/client/app/onboarding/**`, `apps/client/app/index.tsx`, `app/start.tsx`, `apps/client/src/cloud/**`, `apps/client/src/onboarding/**`; in `~/Claude/sotto-cloud`: `src/auth/**`, `src/static.ts`, `src/app.ts` (route registration only), `docs/**`, `vendor/sotto` pin bump at the end; tests beside them. Nothing in `app/(tabs)`, `app/settings`, `app/voice`, `app/reader`, or the landing (other lanes).

Directives:
1. Failing tests first (sotto-cloud vitest for route behaviour; client tests for the redirect logic).
2. `/account?intent=start` renders a create-account framing ("Create your free account": email, what you get, no card); `/account` renders returning-user framing with a "New here? Start free" switch. Both use the existing magic-link request. States: sending, sent (check your email, resend after a cooldown, change address), error (specific), cancel back to where the learner came from. Never reveal whether an email is registered (keep the existing oracle protection).
3. Return destination: carry `returnTo` (same-origin path only, validated) through the magic link's verify redirect so the learner lands where they were; default: un-onboarded → `/onboarding`, onboarded → `/(tabs)/home`.
4. Apple: hide the button on every platform unless the server's health/config says the route is registered; decide whether registering `registerAppleSignIn` is safe to ship now given the comment at `routes.ts:263-278`; if you register it, add the tests that justify the comment's concern being addressed; if not, say why in the report. Write `docs/google-sign-in.md` (sotto-cloud) as the follow-up plan needing Noel's OAuth client; no fake Google button.
5. Onboarding: after sign-in (or on "Try a sample"), the wizard asks four things on separate steps with the current fast-path proposal as defaults: interface language; language to learn; level with a "not sure?" helper (three sample sentences per level to pick from); explanation language. Changing the learning language never changes the interface language (already true; add a test). No tutor-mode step; a final screen recommends one book and links to the library and "Set up the tutor later in Settings".
6. Account area (`/account` when signed in): email, plan line (free or the plan, with Manage/See plans), sign out, delete account (existing flow), a line stating where reading data lives (this device; account sync is not a feature yet).
7. Guest data: on the paid origin, a guest who onboarded and then signs in keeps their local progress (same origin, same IndexedDB): verify and test. Cross-origin (free → paid) cannot carry data; the landing says so (lane A); document it in `docs/`.
8. Static routing on sotto-cloud (`src/static.ts`): `/account`, `/onboarding`, `/settings`, `/voice/*`, `/reader/*` must serve the app shell for direct navigation and refresh; test it.
9. Secrets: never print tokens, cookies, or env values; never read `.env`. Use the stub billing mode and a local Postgres/SQLite per sotto-cloud's README for tests and the Playwright walk; the magic link URL is read from the server log in the walk, never from a mailbox.

Proof: sotto-cloud and client tests green; isolated `pnpm check` on sotto-cloud (clone + submodule restore per LEDGER run 4/5 traps); Playwright walk on a local sotto-cloud: Start free → sent → follow the logged link → onboarding (four steps) → recommendation → library; Sign in as returning → home; cancel and error states; refresh on `/account` and `/onboarding`. Screenshots 375/1440 in `~/Claude/sotto-run7-recon/C/`. No deploy: end the report with the exact `fly deploy` line and what Noel should check after.

Stop when: both repos committed and pushed (path-scoped), vendor pin bumped and lockfile regenerated (`pnpm install --lockfile-only` after the bump), `planning/run7/C-report.md` written. Escalate when: anything would require a Fly deploy, an Apple or Google console action, or a change to Stripe.
