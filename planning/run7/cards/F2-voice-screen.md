# Lane F2: the tutor feels like a conversation

Task: redesign `app/voice/[bookId].tsx` into a conversation screen: transcript, one control cluster with visible mic/speaker/session controls, truthful states, in-place input-mode switching, text fallback, specific recovery, and a spoken opening; tune the prompt for short spoken turns grounded in the passage.

Inputs: `planning/run7/PLAN.md` (CONFIRM 27); `~/Claude/sotto-run7-recon/scout-T-tutor.md` §3 (state machine, captions, the pttDisabled contradiction marked UNKNOWN), §4 (prompting), §5 (errors and what the UI shows today), §8 (live run); `planning/KICKOFF-7-FABLE.md` §"Make the tutor feel like a conversation"; `planning/design/DESIGN.md`; `apps/client/app/voice/[bookId].tsx`, `src/voice/useVoiceSession.ts`, `passage.ts`, `toolContext.ts`, `packages/core/src/prompt.ts`; `planning/BUGS-TUTOR-RUN5.md`. Lane F1 publishes new error codes in `planning/run7/F1-report.md` (read it when it appears; until then, render the existing codes and leave a switch for the new ones). Lane E publishes `useOwnProviderStatus()` in `planning/run7/E-report.md`.

Noel's words: a small connecting/listening/thinking indicator in the corner far from the action; "Enable push-to-talk in settings to speak" with nothing to press; captions at the bottom that do not read as a conversation; the reply came as text, not speech; "I want the tutor to be like a real human tutor experience."

Owned files: `apps/client/app/voice/**`, `apps/client/src/voice/useVoiceSession.ts`, `passage.ts`, `toolContext.ts`, `packages/core/src/prompt.ts` and its test, new `src/voice/ui/**` components, tests beside them. NOT `sessionManager.ts`, `packages/voice` (F1), NOT settings screens (E).

Directives:
1. Step 0, reproduce or rule out the contradiction: instrument state transitions (console table) in a Playwright run on the browser or local path and record whether `listening` and the `pttDisabled` caption can co-occur; if yes, find the path; if no, explain the recording (e.g. the mute button) and remove the dead-end caption either way: the caption becomes an in-place control ("Hold to talk" / "Open mic" toggle) that writes `preferences.turnDetection` without leaving the screen.
2. Layout (375 and 1440): passage context card at top (title, the current sentence window, "Change passage" back to the reader); transcript in the middle as learner/tutor turns (from `session.captions`, all of them, scrollable, latest visible, a "not spoken" marker with Replay where F1 flags it); one control cluster at the bottom: mic button whose ring shows ready / connecting / listening / thinking / speaking / muted, a speaker toggle, End, and the input-mode control with a one-line instruction beside it; a text field fallback that sends a turn (`session.sendText` if it exists in F1's manager, else add the hook call and coordinate through the report).
3. States: never show "Listening" unless the gated state says so; never leave "connecting" or "thinking" after an error (timeouts surface an error with Retry). Muted is a visible state on the mic ring. Reduced motion respected.
4. Opening: when the session becomes ready, the tutor speaks one short invitation grounded in the passage (one sentence in the learning language, at level); it also appears in the transcript.
5. Recovery panels by code: mic denied (how to allow it on this browser + Try again), no input device, connection lost (Retry keeps the transcript), provider rejected the setting (link to the guided flow at `/settings/openai-key`), quota (what it means + link), blocked playback (a "Tap to hear the tutor" button that calls the resume action), plan required (See plans). No indefinite spinners.
6. Prompt (`packages/core/src/prompt.ts`): concise spoken turns (two sentences), one follow-up question, proportionate correction (not every turn), passage-only facts, learning language at level with the explanation language on request; keep `bookTitle` receiving the real title (scout-T §4 flags the id being passed). Unit test the assembled instruction for the discuss mode.
7. Settings entry: a quiet header icon → `/settings`.
8. The Provence exchange: script it in Playwright with F1's fake provider mode if available (learner text: "Qu'est-ce que c'est, la Provence ? Est-ce en France ?"), assert the reply is rendered in the transcript and the audible probe counts samples, and screenshot each state.

Proof: tests green; state screenshots in `~/Claude/sotto-run7-recon/F2/`; the step-0 log and verdict; the scripted exchange result; keyboard operation of the controls (tab order, Space/Enter on mic, Escape ends nothing without confirm).

Stop when: committed, pushed, `planning/run7/F2-report.md` written. Escalate when: you need a change in `sessionManager.ts` or `packages/voice` (write the exact interface you need; F1 or the orchestrator adds it).
