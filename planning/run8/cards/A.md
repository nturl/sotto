# Lane A — Books: cover system, tile, shelf, ribbon, page label (wave 1, Opus)

**Task.** Replace the pack-SVG / eight-illustration covers with the typographic cover system, turn `Rail` into the shelf, put the ribbon on the current book, and replace the progress bar with the mono page label. Widen `LibraryBook.categories` to the seven core categories. This lane ships the contract every other lane consumes, so land it in two commits: (1) `Cover` + palette + tokens, (2) tile + rail + data seam.

**Inputs.** `planning/run8/PLAN.md` decisions 1–6, 13, 14. Mockup: `.cv`, `.cv:before` (spine), `.cv .g` / `.g.sym` (mark), `.cv .t` / `.a` / `.lv`, `.shelf`, `.book`, `.book .tt/.au/.pg`, `.ribbon`, `.seeall` in `planning/design/app-mockup-v2.html:53-74`. RECON.md §1 (rail derivation, progress unit), §5 (Text roles, two colour paths), §7 (test convention), §10 and risks 1, 2, 3, 10.

**Owned files.** `apps/client/src/ui/Cover.tsx`, new `apps/client/src/ui/coverPaper.ts` (+ `.test.ts`), new `apps/client/src/ui/progressLabel.ts` (+ `.test.ts`), `apps/client/src/ui/BookTile.tsx`, `apps/client/src/ui/Rail.tsx`, `apps/client/src/ui/railView.ts`, `apps/client/src/ui/Rail.test.ts`, `apps/client/src/ui/data.ts`, `apps/client/src/ui/data.test.ts`, `apps/client/src/ui/dev/fixtures.ts`, `apps/client/src/state/selectors.ts` (+ test) only if `byCategory` needs it, `packages/core/src/theme.ts`, `packages/core/src/theme.test.ts`, and every OTHER `<Cover` call site except `src/ui/DailyStoryCard.tsx` (lane B), `app/(tabs)/library.tsx` (lane C), `app/reader/[bookId].tsx` (lane D): i.e. `app/book/[bookId].tsx`, `app/library/search.tsx`, `app/(tabs)/vocabulary.tsx`, `src/ui/SessionBar.tsx`, `app/onboarding/done.tsx`, `app/review.tsx` and any other you find with `grep -rn '<Cover' apps/client`. i18n keys via the script only.

**Output.**
- `Cover`: props per PLAN.md decision 2. Rendered with plain `View`/`Text` (no SVG): paper background from `coverPaper`, radius 2, 3px spine strip on the left, the mark top-left (Fraunces 300, size `64 * width/120`, tracking -0.04em; glyph marks at `44 * width/120`), title bottom-left (Fraunces 300, `13 * scale`, 3 lines max), author (Inter, `8 * scale`, uppercase, tracking 0.14em), level stamp (mono `9 * scale`, 1px border in the text colour, radius 2, right 8 bottom 8). Peach cutout 6px via the existing `cutout` props. `svgUrl` is used only when `title` is empty. Styles via `useTheme()` (risk 10).
- `coverPaper.ts` (RN-free): `coverPaper(book) → PaperName`, `paperInk(paper) → 'ink' | 'canvas'`, `coverMark(book) → { kind: 'initial' | 'glyph'; text }`, the article-stripping list from PLAN.md decision 4, and a stable string hash. Tests first: same id → same paper and mark; each collection maps only into its triple; ink/canvas per paper; initial stripping for "Le Petit Chaperon rouge" → "P", "L'Oiseau bleu" → "O", "The Red-Headed League" → "R", "塞甘先生的羊" → "塞"; glyph branch reachable.
- `progressLabel.ts` (RN-free) per decision 5, with tests for 0, mid, 1, and minutes rounding.
- `BookTile`: width 120/104, cover, title (Fraunces 400 14/1.3 ink, one line), author (Inter 12.5 ink2, one line), the mono line (`p. X of Y` via new key `tile.pageOf` = "p. {page} of {pages}", or the existing minutes key), optional `ribbon` prop (accent 12x44, top -6, right 14, chevron notch) and no progress track. Keep the `caption` prop for imported books. Drop the desktop grid sizes.
- `Rail`: shelf per decision 1; `ribbonBookId?: string`; "See all" is a text link (`role="uiButton" size=14 color="ink2"`, min height 40, no background); delete `useBookGridTier` (grep its users: `library/search.tsx` is yours). `railView.ts` keeps the empty-state decision; add `pickRibbon(books, ribbonBookId)` or equivalent pure helper if you need one, with a test.
- `data.ts`: `categories` = core `BookCategory[]` unchanged from the pack; `byCategory(core)`; new `currentBookId` per decision 6; `cover: CoverArt` and `hashCover` removed; `svgUrl` kept.
- Theme tokens per decision 3 (both schemes; keep `theme.test.ts` green — it may assert key parity).

**Proof.** Failing tests first (`coverPaper.test.ts`, `progressLabel.test.ts`, `Rail.test.ts` for the ribbon pick). `pnpm --filter @sotto/client test`, `pnpm -r typecheck`, `pnpm lint`, prettier on touched files. Then `cd apps/client && node ~/Claude/sotto-run8/shots.mjs ~/Claude/sotto-run8/A/` and compare `1440-home.png` / `375-home.png` against the mockup's Continue reading shelf (frame 1 and phone 1): six paper colours visible across the rails, spine, mark, stamp, shelf hairline, one ribbon on the Chèvre, "p. 3 of 7" under it. Also shoot `/book/fr-chevre-de-m-seguin` at 375 (cover 180x270 renders typographically). Report `planning/run8/A-report.md`.

**Permissions.** Read anything. Write only owned files. Commit `run8(A):` by explicit path, push. No deploys.

**Stop when.** Both commits pushed, tests green, shots taken and compared, report written. Lanes B and C start from your second commit.

**Escalate when.** The 0..1 progress unit turns out wrong; `theme.test.ts` forbids new keys; a `<Cover` call site is in a file another lane owns (leave it, list it in the report); anything takes two serious attempts.
