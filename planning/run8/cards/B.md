# Lane B — Home order, Today's story spread, plan row to Settings › Account (wave 2, Opus, after lane A's second commit)

**Task.** Rebuild `app/(tabs)/home.tsx` to the mockup's Desktop Home (frame 1) and Phone Home (phone 1): Continue reading first, Today's story as a spread with Read / Listen / About, Recommended for {level}; the plan nag leaves Home for Settings › Account.

**Inputs.** PLAN.md decisions 1, 6, 8, 10, 13, 14. Mockup `.titlerow`, `.icons`, `.ib`, `.rail`, `.railhead`, `.spread`, `.spread .l/.r`, `.spread h3`, `.meta`, `.syn`, `.btns`, `.btn.cta/.sec/.ghost`, phone overrides `.pmain .spread*`, `.pmain .btn` (`app-mockup-v2.html:43-53, 75-86, 134-149, 193-243, 317-343`). RECON.md §1, §6, risks 4, 5. Lane A's report (`planning/run8/A-report.md`) for the final `Cover`/`Rail`/`BookTile` API and `Library.currentBookId`. Lane E's report for glyph names.

**Owned files.** `apps/client/app/(tabs)/home.tsx`, `apps/client/src/ui/DailyStoryCard.tsx` (rename to `TodaysStorySpread.tsx` if you like; delete the old file in the same commit), `apps/client/src/ui/PaywallNagRow.tsx`, `apps/client/src/paywall/nagOnce.ts` and its test (only if you retire the once-per-session claim), `apps/client/app/settings/index.tsx` (the Account group only), `apps/client/src/ui/SectionEyebrow.tsx` (only if needed). i18n keys via the script only.

**Output.**
- Title row: display "For you"; icon buttons 40px ink: Search (→ `/library/search`) and Settings (→ `/settings`) on desktop; Search only on phone. The gift button and the countdown are gone (`home.gift` key may stay unused; do not delete keys).
- Order inside the `none` branch: (1) Rail "Continue reading" (rename `home.rail.continue` en value from "Resume" to "Continue reading" and give the eight real translations), `ribbonBookId={library.currentBookId}`, no See all, hidden when empty; (2) rail head "Today's story" + mono "Changes at midnight" on the right (desktop only), then the spread: surface card radius 10 hairline; left column 184 (phone 132) surface-2 with hairline right holding the cover 120x180 (phone 96x144) with the 6px cutout; right: meta mono "{minutes} min · {level} · {language}" (phone: "{minutes} min · {level}"), title Fraunces 300 30 (phone 22), synopsis 15/1.5 ink2 max 46ch (desktop only), buttons Read (primary: accent fill, **ink** label, 4px ink cutout, press → 2px), Listen (surface-2), About this book (ghost, ink2, desktop only). Read → reader; Listen → reader with narration started (read how the reader takes a start-narration intent: existing query param, `read/[bookId]` route, or store flag; if none exists, add `?listen=1` to the reader as the smallest change — that touches lane D's file, so instead write the intent into the store/session bar and note it, or wait for D's commit and add a 5-line param read there, saying so in the report); About → `/book/{id}`. Press animation stays the cutout press (reuse `usePressAnimation`). No gradient anywhere. (3) Rail "Recommended for {level}" with See all → `/library?level={level}`; (4) Rail "Your books" (`library.yourBooks`) only when non-empty. "New releases" leaves Home. The packs banner branches stay as they are.
- Plan row: remove `PaywallNagRow` from Home. In `settings/index.tsx`, inside the existing Account group (cloud-enabled only), add one row with the existing `paywall.nag.copy` caption and `paywall.nag.cta` accent action → `/paywall`, shown whenever the learner has no plan (read `useMe`/cloud state the same way the group's plan value does). Retire `claimNagSlot` if nothing else uses it; keep its test file consistent.

**Proof.** Failing test first for any pure logic (e.g. `homeSections.ts` deciding the section list from `{continue, daily, recommended, yourBooks}` counts, RN-free). `pnpm --filter @sotto/client test`, `pnpm -r typecheck`, `pnpm lint`, prettier. Live: `node ~/Claude/sotto-run8/shots.mjs ~/Claude/sotto-run8/B/`; compare `1440-home.png` / `375-home.png` against frame 1 / phone 1 (order, spread, no nag, no countdown). Also click Read and Listen via a short Playwright script and record where each lands (URL + whether narration is playing: the Pause button becomes visible). Settings › Account cannot be seen on the local OSS build (cloud disabled); prove the row with a unit test on its visibility decision and by reading the JSX; say so. Report `planning/run8/B-report.md`.

**Permissions.** Read anything. Write only owned files. Commit `run8(B):` by explicit path, push. No deploys.

**Stop when.** Pushed, tests green, shots compared, Read/Listen/About destinations recorded, report written.

**Escalate when.** "Listen" needs a reader change larger than a param read; `Cover`/`Rail` API from lane A does not match PLAN.md (do not patch lane A's files — report); two serious attempts at anything.
