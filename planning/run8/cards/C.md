# Lane C — Library: level scale, collection links, inline search, `?filter=` (wave 2, Opus, after lane A's second commit)

**Task.** Rebuild `app/(tabs)/library.tsx` to the mockup's Desktop Library (frame 2) and Phone Library (phone 2): one hairline-segmented level scale, plain collection links, an inline search field, shelves. No pills. Keep the URL filter from run 7 and add the level param.

**Inputs.** PLAN.md decisions 1, 3, 6, 9, 13, 14. Mockup `.controls`, `.scale`, `.scale span(.on)`, `.coll`, `.coll a(.on)`, `.search`, `.titlerow .meta`, phone overrides `.pmain .controls/.scale/.search`, `.coll` at line 357 (`app-mockup-v2.html:87-97, 154-156, 245-281, 352-374`). RECON.md §2 in full (especially the run-7 ScrollView style trap at `library.tsx:133-141`), §8 (screenshots.mjs uses the fr placeholder "Rechercher…"), risk 6, 11. Lane A's report for `Library.byCategory(core)`, `currentBookId`, the shelf.

**Owned files.** `apps/client/app/(tabs)/library.tsx`, new `apps/client/src/ui/LevelScale.tsx`, new `apps/client/src/ui/libraryFilters.ts` (+ `.test.ts`, RN-free: param parse/serialize, legacy aliases, rail composition), `apps/client/src/ui/Chip.tsx` (delete only if `grep -rn "from './Chip'\|from '../ui/Chip'" apps/client` shows no other user; otherwise leave it), `apps/client/app/library/search.tsx` (only if the inline field changes what the search screen must do), `apps/client/src/state/selectors.ts` + test only for `isFilterEmpty` if its shape changes. i18n keys via the script only.

**Output.**
- Title row: display "Library" + mono meta right "{language} · {n} books" (new key `library.count`; keep the plural simple, e.g. "{n} books" / "{n} livres"; zh has no plural). The import (+) button stays where it is when the server is reachable; the search icon button goes (the field replaces it).
- Controls row (desktop, one row, gap 28, wrapping): `LevelScale` — All, A0…C1, one bordered group (`hairline2`, radius 10), segments mono 12 tracking 0.06em padding 8 13 with `hairline2` dividers, selected = ink fill + surface text, `accessibilityRole="radio"`/`radiogroup`; collection links — Everything, then one link per core category that has at least one book in this locale (labels via existing `library.rail.*` / new `library.collection.*` keys: tales "Tales", fables "Fables", adventure "Travel", classics "Classics", folk "Folk", idioms "Idioms", daily "Everyday"), plus "Your books" when imports exist; each link ink2, active = ink with a 1.5px inset underline, min height 40; search field right-aligned 240 (surface-2 radius 10, glyph + placeholder `library.searchPlaceholder`). Phone: search field full width under the title, then the scale (padding 8 9, mono 11) in a horizontal ScrollView using the run-7 pattern (style on the outer View, NOT the ScrollView), then the collection links row.
- Filtering: `?filter=` (collection) and `?level=` per decision 9, both via `router.setParams`, invalid → unset; legacy `voyage`→`adventure`, `contes`→`tales`, and a level value in `filter` → moved to `level` and rewritten. Rails: with no filters, one shelf per core category that has books (in the order above), then "All books" without See all; a collection filter → that one shelf; a level filter → each category shelf restricted to that level (empty ones hidden), `library.rail.all` restricted too; both → one shelf. `ribbonBookId={library.currentBookId}` on every shelf. See all on a category shelf → `?filter=<category>` (keeping `level`). Inline search: typing a query (local state) replaces the shelves with one "Results" shelf from `library.search(query)`; empty → `library.noResults`. The `/library/search` route stays and must still work.
- Empty states: the packs banner and the run-7 `packs.status.emptyFilter` + "Clear filters" (now clears both params) keep working.

**Proof.** Failing `libraryFilters.test.ts` first: parse `{filter:'voyage'}` → `{collection:'adventure', level:undefined}`; `{filter:'A2'}` → `{collection:'all', level:'A2'}`; serialize round trip; rail composition for the four cases above with a hand-built book list. `pnpm --filter @sotto/client test`, `pnpm -r typecheck`, `pnpm lint`, prettier. Live: `node ~/Claude/sotto-run8/shots.mjs ~/Claude/sotto-run8/C/`; compare `1440-library.png` / `375-library.png` against frame 2 / phone 2. Then a short Playwright script: click A2 → URL has `level=A2`, reload → still selected; click Fables → `filter=fables&level=A2`; direct link `/library?filter=voyage` → Travel selected and URL rewritten; back button restores the previous state; type "chèvre" in the field → one Results shelf. Screenshot each state at 375 into `~/Claude/sotto-run8/C/`. Report `planning/run8/C-report.md`.

**Permissions.** Read anything. Write only owned files. Commit `run8(C):` by explicit path, push. No deploys.

**Stop when.** Pushed, tests green, the five URL behaviours proven, shots compared, report written.

**Escalate when.** Lane A's `byCategory` does not take core categories (report, do not patch); the phone scale cannot stay at 36px without the run-7 trap; two serious attempts at anything.
