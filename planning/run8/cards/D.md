# Lane D — Reader: measure, plain tokens, transport under the passage, panel order (wave 1, Opus)

**Task.** Bring `app/reader/[bookId].tsx` and `src/ui/reader/*` to the mockup's Desktop Reader (frame 3) and Phone Reader (phone 3): 640 passage measure centered, plain tokens, transport as a thin bar under the passage, the panel in the fixed order ending with "Talk about this passage".

**Inputs.** PLAN.md decisions 7, 8, 11, 12, 14. Mockup `.reader`, `.rd`, `.passage`, `.w.sel`, `.w.saved`, `.transport`, `.play`, `.segs`, `.panel`, `.word/.gl/.ph`, `.spk`, `.pactions`, `.save(.done)`, `.cap`, `.ex`, `.talk`, phone `.prd`, `.sheet`, `.handle` (`app-mockup-v2.html:98-133, 157-167, 283-311, 383-399`). RECON.md §3 in full, §8 (hosted.mjs `firstWordCenter()` depends on the dotted underline you are removing), risks 7, 8.

**Owned files.** `apps/client/app/reader/[bookId].tsx`, `apps/client/src/ui/reader/*`, `apps/client/src/ui/tokens.ts`, `apps/client/src/ui/Sheet.tsx` (only if the sheet needs a change), `apps/client/e2e/hosted.mjs` (only `firstWordCenter()` and any selector you must move to a testID), `apps/client/e2e/voice-live.mjs` and `audible-probe.mjs` (only selectors that your change would break). i18n keys via the script only. The `<Cover` call in the reader (recommendations / completion) moves to lane A's contract `<Cover book={…} width height cutout />`; if `Cover.tsx` does not yet accept `book` when you get there, leave that one call unchanged and say so in the report.

**Output.**
- Desktop (`width >= DESKTOP_BREAKPOINT` imported from `Shell.tsx`, replacing the 900 literal): passage column `maxWidth: 640` centered in the left area, top row = chapter mono left ("{title} · Chapter {n} of {m}" — reuse the existing chapter label if it already reads like that, else add a key), 40px icon buttons right: Settings, Close. The header mic button leaves; its action becomes the panel's last row. Transport: hairline top, `[elapsed mono] [prev · -10 · play ring 44 accent 2px outline · +10 · next] [remaining · speed]`, then the segmented row (3px, surface-2, done = ink), all inside the 640 measure. Right panel 360, surface, hairline left, padding 28, sticky on web as today.
- Tokens: no underline at all (`peachUnderline` deleted from `tokens.ts` if nothing else uses it); selected = peach at 55% (`peachSelection` becomes `withAlpha(colors.peach, 0.55)`), radius 2; saved = the marker as today. Speech fill unchanged.
- Panel order per decision 11, with testIDs: `reader-panel-word`, `reader-panel-gloss`, `reader-panel-form`, `reader-panel-speaker`, `reader-panel-save`, `reader-panel-details`, `reader-panel-report`, `reader-panel-passage`, `reader-panel-your-words`, `reader-panel-talk`. Word: Fraunces 400 28 (`role="heading" size={28}`); gloss ui 16 ink2; form line mono 12 tracking 0.04em only when the token has lemma/POS data (read `packages/core/src/models.ts` Token — if no such field exists, omit and say so); speaker ring 44 accent outline at the block's top-right; Save = surface-2 radius 10, 500 14, bookmark glyph, saved state = mark fill + 1.5 ink border + 4px ink cutout; Details / Report = caption ink2 with 40px hit height; "In this passage" = eyebrow (mono uppercase) + the containing sentence with the word carrying the mark (Fraunces 15/1.5); "Your words in this book" = eyebrow + saved words for this book joined by " · " (omit the block when none); "Talk about this passage" = surface-2 radius 10, mic glyph, 500 15, pinned to the panel bottom (`marginTop: auto`), calls the existing `talkAboutPassage`. Empty state (no selection) keeps `reader.emptyState` and still shows the Talk row.
- Phone: the sheet keeps the same order, scrolls inside itself, and ends with the Talk row then the transport (mockup phone 3). The sheet is visible with the empty state + Talk + transport when nothing is selected only if that does not fight the run-7 `bottomOffset` logic; otherwise keep today's visibility rule and say so.
- `hosted.mjs` `firstWordCenter()` → select `span[data-tokenid]` (already emitted by `SelectableSpeechText.tsx:119`). Run the reader half of hosted.mjs locally if the script allows a BASE_URL of `http://localhost:8081`; otherwise prove the selector with a 10-line Playwright script under `~/Claude/sotto-run8/D/`.

**Proof.** Failing tests first for any pure logic you extract (e.g. `panelOrder.ts` / `sentenceForToken` if you add one; `selection.test.ts` stays green). `pnpm --filter @sotto/client test`, `pnpm -r typecheck`, `pnpm lint`, prettier. Live: `node ~/Claude/sotto-run8/shots.mjs ~/Claude/sotto-run8/D/` and compare `1440-reader.png` / `375-reader.png` to frame 3 / phone 3. Then on the real Metro: `SOTTO_SCREENSHOT_DIR=~/Claude/sotto-run8/D node apps/client/e2e/voice-live.mjs` (explain a word, save it) and `node apps/client/e2e/audible-probe.mjs` (talk about this passage still opens the tutor; 10/10 or write the assertion that missed). Paste the tail of each run. Report `planning/run8/D-report.md` with the panel order as rendered (list it from the DOM, not from your intent).

**Permissions.** Read anything. Write only owned files. Commit `run8(D):` by explicit path, push. No deploys.

**Stop when.** Pushed, tests green, both e2e scripts pass or their misses are written up, shots compared, report written.

**Escalate when.** The sheet's height/offset logic cannot host the longer content without a Sheet.tsx redesign (write the smallest change and flag it); `audible-probe` fails on something outside the reader; two serious attempts at anything.
