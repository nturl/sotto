# Lane E — Nav glyphs, sidebar, Glyphs.tsx additions, DESIGN.md (wave 1, Opus)

**Task.** Put the mockup's four ink glyphs on the tab bar (active accent), align the sidebar with the mockup, add the glyphs other lanes will need to `Glyphs.tsx`, and record the v2 system in `DESIGN.md`.

**Inputs.** PLAN.md decisions 7, 8, 12, 13, 14. Mockup `.tabs`, `.tab`, `.tab.on`, `.side`, `.nav a(.on)`, `.wordmark`, `.ib` (`app-mockup-v2.html:36-49, 150-153, 344-349`), the glyph paths at lines 205-206 (search, settings), 289 (settings-sun variant, close), 298 (prev/next), 304 (speaker), 305 (bookmark), 308 (mic), 345-348 (the four tabs). `APP-V2-SPEC.md` "Found while verifying" and the ban list. RECON.md §4, §5, risk 12 (DESIGN.md section names are cited in code comments — keep them). `planning/design/DESIGN.md` in full.

**Owned files.** `apps/client/src/ui/TabBar.tsx`, `apps/client/src/ui/TabBar.test.ts`, `apps/client/src/ui/Sidebar.tsx`, `apps/client/src/ui/Sidebar.test.ts`, `apps/client/src/ui/navRows.ts`, `apps/client/src/ui/Glyphs.tsx`, `apps/client/src/ui/IconButton.tsx` (only if the 40px ink button needs a variant), `planning/design/DESIGN.md`.

**Output.**
- `Glyphs.tsx`: four tab glyphs with the mockup's exact paths (`BookOpenGlyph` for For you, `ShelvesGlyph` Library, `BookmarkGlyph` Vocabulary — update the existing one if its path differs, `GearGlyph` Settings — the mockup's `.tab` gear is the sun-style path at line 348; keep the existing `SettingsGlyph` for the title-row button, whose path is at line 206). Also make sure these exist with the mockup's paths for other lanes: `SearchGlyph`, `CloseGlyph`, `SpeakerGlyph`, `MicGlyph`, `SkipPrevGlyph`, `SkipNextGlyph`, `PlayGlyph` (12px triangle). Stroke 1.5 for tab glyphs (round caps/joins), 1.6 for 20px icon buttons, 1.8 for the speaker/save 15-18px glyphs, as in the mockup CSS. Migrate `Glyphs.tsx` off the module-scope `themeColors` proxy to `useTheme()` inside `GlyphShell` (risk 10).
- `TabBar`: four tabs, 22px glyphs, label 11 (500 when active), active accent for glyph and label, inactive ink2, surface bg, hairline top, padding 10 8 22 (bottom inset replaces 22 on device). Replace the `900` literal with `DESKTOP_BREAKPOINT` from `Shell.tsx`. `TabBar.test.ts` extends `navRows` with the glyph name per row so the pairing is testable RN-free (failing test first).
- `Sidebar`: match the mockup (`.side`: 24 20 padding, wordmark Fraunces 300 26, rows 9 12 padding radius 10 size 15, active = surface-2 + ink 500, Settings pinned bottom). Text rows, no glyphs, as in the mockup.
- `DESIGN.md` (keep every existing section heading; append or edit in place): (a) Tokens › Color: `accent` job widened to "CTA fill, active tab, and the ribbon on the book you are in"; `paper.*` six cover colours with sage #6E9A7C; `hairline2`; (b) the four verify findings: CTA label is ink (cream on accent 3.45:1, ink 5:1), ink-3 never for text under 13px (mono metadata uses ink-2), sage lightened to #6E9A7C so ink text clears 4.5:1, text links carry a 40px hit height; (c) selected word fill is peach at 55% (18% was invisible on canvas); (d) a "Signature devices" entry for **the shelf** (1.5px hairline under every rail, one cutout per cover, ribbon on the current book, "p. X of Y" mono instead of a bar); (e) a "Covers" section describing the typographic system (paper by collection, spine, one mark, Fraunces 300 title, small-caps author, mono level stamp; no repeated motif); (f) Home/Library/Reader screen entries updated to the v2 order (Continue reading first, Today's story as a spread with Read / Listen / About, Recommended; level scale + collection links + inline search; passage 640, transport under the passage, panel order); (g) Don't: "No pill chips anywhere (filters, See all, actions); no progress bars on covers; no second shadow on tiles; no countdown on the daily story." Retire DESKTOP.md's grid tiers with one line pointing at the shelf.

**Proof.** Failing `TabBar.test.ts` first. `pnpm --filter @sotto/client test`, `pnpm -r typecheck`, `pnpm lint`, prettier. Live: `node ~/Claude/sotto-run8/shots.mjs ~/Claude/sotto-run8/E/`; compare the tab bar in `375-home.png` / `375-library.png` with phone frames 1–2 and the sidebar in `1440-*.png` with frames 1–2. Zoom-crop the tab bar (Playwright `clip`) so the glyphs are legible. Report `planning/run8/E-report.md`, including the list of glyphs added/changed so lanes B/C/D can use them.

**Permissions.** Read anything. Write only owned files. Commit `run8(E):` by explicit path (glyphs first, then tab bar/sidebar, then DESIGN.md), push. No deploys.

**Stop when.** Pushed, tests green, shots compared, DESIGN.md updated, report written.

**Escalate when.** A code comment cites a DESIGN.md section you would have to rename (keep the name, add a sub-heading instead); two serious attempts at anything.
