# Lane A — the transcript gate and capture hygiene

Task: make the browser cascade never hand the LLM a transcript the learner did not say: hallucinated fillers, near-silent segments, self-echo of the tutor's own voice, clipped push-to-talk openings.

Inputs: `packages/voice/src/browser-cascade/worker.ts` (`transcribeSegment`, `handleFrame`, the `ptt` case), `stt-fallback.ts` (+its test, the pattern to copy), `vad.ts` (`EnergyVad`, `SpeechBuffer`), `protocol.ts`, `packages/core/src/prompt.ts` (`sttLanguageHint`, currently unused by this worker), `planning/BUGS-TUTOR-RUN5.md` #3 and #4, `docs/evidence/browser-tutor-stt-regression-2026-09-05.log`.

Output:
1. New `packages/voice/src/browser-cascade/transcript-gate.ts` + `packages/voice/test/browser-cascade-transcript-gate.test.ts`, pure: `classifyTranscript(text, { durationMs, rms, locale })` → `{ verdict: 'ok' | 'hallucination' | 'too_short' | 'silent', reason }`. Hallucination list: Whisper's known silence outputs, case- and punctuation-insensitive, at minimum: "you", "thank you", "thanks", "thanks for watching", "thank you for watching", "bye", "okay", "hmm", "mm", "uh", ".", "…", "subtitles by …", "[music]", "(music)", "[blank_audio]", plus any transcript of ≤ 2 words when the segment's speech RMS is under a threshold you pick from the VAD's 0.02 and justify in a comment. Also fold `isDegenerateTranscript` in so there is one gate. Short real words must pass: "no", "yes", "why", "the dog" at normal RMS.
2. `worker.ts` wiring: compute duration and peak/mean RMS of the segment (a helper in the gate module, tested); run the gate BEFORE `runTutorTurn`; on a non-ok verdict post `caption` from the tutor, final, with a short fixed line "I didn't catch that. Could you say it again?" (English only for now; the caption text lives in the gate module as a constant so lane D can map it to i18n later; say this in your report) and a `metric` named `stt_rejected` with the verdict; state back to `listening`. Keep the wasm fallback logic exactly as is.
3. Push-to-talk: on `ptt active` keep the pre-roll instead of clearing it (`buffer.start()` without `clear()`; read `SpeechBuffer` and decide whether the pre-roll should be capped to ~300 ms for PTT; test it). On release, a segment under 250 ms of audio is dropped with the same `stt_rejected` metric (verdict `too_short`), no caption.
4. Half-duplex in auto mode: while the worker is in the `speaking` state (track it in `SessionState` from `speakSentence`/`audio_end`), the VAD needs a higher threshold or a sustained run to fire `speech_start` (barge-in must still work for a real voice; the tutor's own voice through a laptop speaker with AEC on must not). Implement as an `EnergyVad` option (`setThresholdScale` or a second threshold) with a test; pick the multiplier from a measurement if you can make one (`~/Claude/sotto-run9/A/`), otherwise 2.0 with the reasoning in a comment.
5. Pass `sttLanguageHint(...)` as Whisper's prompt if `@huggingface/transformers` 4.x's ASR pipeline accepts a text prompt for whisper (check its source in `node_modules`, do not guess); if it does not, say so in the report and leave it.

Proof: failing tests first; `pnpm --filter @sotto/voice test` green with the new tests listed; a worker bundle build passes; a browser check of your worktree's export (port 8091) where a fake-mic WAV of 2 s of silence followed by nothing produces NO learner caption and the "didn't catch that" line, and a WAV of "Tell me more about the gray husky dog." produces a learner caption containing "dog" (adapt lane E's approach: `apps/client/e2e/browser-tutor.mjs` shows the flags and WAV synthesis; if the fake mic delivers no frames, prove the gate through unit tests and say so).

Permissions: owns `transcript-gate.ts`, its test, `vad.ts` and `browser-cascade-vad.test.ts`, the three named regions of `worker.ts`, `protocol.ts` only to add the `stt_rejected` metric name if metric names are typed, `planning/run9/A-report.md`. Not the prompt, not `speakSentence`, not `llm-turn.ts`, not the client.

Stop when: tests green, typecheck and lint clean, report committed with the browser evidence or the honest statement that it was unit-only.

Escalate when: the transformers.js pipeline API differs from what `transcribeSegment` assumes in a way that needs a version bump (write the finding, do not bump).
