# Lane B — the reply contract for a 2B model

Task: make the browser cascade's Discuss replies obey the contract (plain prose, at most two sentences of answer plus one follow-up question, no filler, no lists, no markdown, no emoji) and make the tutor ask for a repeat when the learner turn is unusable, without changing what the paid and local providers send today.

Inputs: `packages/core/src/prompt.ts` (+`packages/core/test` or wherever its tests live: find them), `packages/voice/src/browser-cascade/llm-turn.ts` (+`packages/voice/test/browser-cascade-llm-turn.test.ts`), `markers.ts` (+test) for the streaming-safe pattern, `worker.ts` (`WebLlmEngine.chat` request object, `makeTurnRunner`), `chunker.ts`. Noel's failing transcript is in `planning/run9/PLAN.md`.

Output:
1. `buildSystemInstruction` gains an optional `compact?: boolean` in `PromptContext`. When true (the worker sets it; every other caller is untouched and a test proves the default output is byte-identical to today's), the stable rules and the `discuss` guidance are rewritten for a small model: short imperative rules near the END of the prompt (recency matters for 2B models), an explicit "Reply in plain sentences only. Never use bullet points, numbered lists, headings, asterisks, or emoji. Never begin with filler such as 'Okay' or 'Let's see'. Two sentences that answer, then one question. If the learner's message is empty, a single word, or does not make sense, do not summarize the passage; ask them to repeat the question in one sentence." Keep the passage word map (tools need it) but consider moving it above the rules. Keep the per-mode guidance for the other three modes working under compact too (read_to_me's `[[reading:]]` marker must survive).
2. New `packages/voice/src/browser-cascade/reply-shape.ts` + test, pure and streaming-safe: `normalizeReplyText(text)` strips markdown bullets/numbering at line starts, `*`/`_`/`#`/backtick emphasis, emoji and pictographs, collapses newlines to spaces, drops a leading filler sentence from a fixed list ("Okay, let's see.", "Okay.", "Sure.", "Great question.", "Let's see."); and `ReplyBudget` that counts sentences as the chunker emits them and reports when the cap (3 spoken sentences in discuss; unlimited in read_to_me; 2 in the other modes) is reached.
3. `llm-turn.ts`: apply `normalizeReplyText` on each released chunk before the chunker (mind partial deltas: a `-` at the start of a not-yet-complete line; use `safeReleaseIndex`-style holdback and test it), and when `ReplyBudget` says the cap is hit, abort the engine stream (the runner already has `signal`; add an internal abort that does NOT mark the turn as barged-in, so the caption still finalizes and history still records the spoken text). Test with the fake engine: a five-line bullet reply becomes prose, three sentences spoken, caption final = what was spoken.
4. `worker.ts`: set `compact: true` in `buildSystemInstruction`, lower `max_tokens` to 160 for discuss and the two feedback modes (keep 400 for read_to_me), and pass mode into the runner's budget. Keep `temperature` unless you show a reason in the report.

Proof: failing tests first; `pnpm --filter @sotto/core test` and `pnpm --filter @sotto/voice test` green; the byte-identity test for non-compact output; then a real-model check: build your worktree's export (port 8092) and drive Discuss through the text fallback with these five learner texts, capturing the tutor's final caption for each (adapt `apps/client/e2e/browser-tutor.mjs`; the text path needs no fake mic): "Tell me more about the gray husky dog.", "you", "Why was there no sun?", "What does fifty below mean?", "stop". Score each: ≤ 3 sentences, prose only, ends with `?` (except after "stop"), and "you" gets a repeat request, not a summary. Paste the five captions in the report. If WebGPU is unavailable headless after two attempts, say so and ship on unit proof.

Permissions: owns `packages/core/src/prompt.ts` and its test, `reply-shape.ts` + test, `llm-turn.ts` + test, the `WebLlmEngine.chat` request literal and `makeTurnRunner` in `worker.ts`, `planning/run9/B-report.md`. Not `markers.ts` (read it, copy its holdback idea), not `speakSentence`, not `transcribeSegment`, not the client.

Stop when: tests green, typecheck and lint clean, the five captions are in the report (or the honest unit-only statement), report committed. If you believe the 2B model cannot meet the contract even with these changes, say so with the captions as evidence and name the smallest alternative you would test next; do not switch models.

Escalate when: `interruptGenerate` on WebLLM leaves the engine unusable for the next turn (the SessionState comment describes a hang); then keep the cap as caption-and-speech truncation without aborting the stream and report it.
