# Lane C — intelligible speech from Kokoro

Task: prove, with a machine-scored round trip, that the audio the browser cascade speaks is intelligible English, and fix whatever makes it not so (dtype/device choice, text handed to Kokoro, sentence length, playback conversion).

Inputs: `packages/voice/src/browser-cascade/worker.ts` (`loadTts`, `speakSentence`, `floatToPcm16`, the `sample` case), `models.ts` (`KOKORO`), `packages/voice/src/transports/web-audio.ts` (`playPcm`), `protocol.ts` (`TUTOR_SAMPLE_RATE`), `chunker.ts`, kokoro-js in `node_modules` (read its README and `src/` for supported dtypes per device, its max input length, its `voice` list, and whether it ships a text normalizer). The landing page already used a Kokoro clip made offline (`planning/LEDGER.md`, "Landing V5" entry) so there is a known-good reference of what Kokoro should sound like.

Output:
1. `packages/voice/scripts/tts-roundtrip.mjs` (Node, kokoro-js on cpu/wasm, then Whisper base via `@huggingface/transformers` on cpu): for a fixed list of tutor-style sentences (include ones with numbers "fifty degrees below zero", quotes, an em dash, a trailing question, a 60-word sentence, a sentence with a `-` bullet prefix and `**bold**`), generate audio per (dtype in q8, fp32, fp16 if offered) and write WAVs to `~/Claude/sotto-run9/C/`, transcribe each, print a table of WER per sentence per dtype (reuse lane E's WER if it has landed on `run9/E`; otherwise write a small one in the script and note the duplication for the orchestrator). This is the objective ear.
2. Browser matrix: adapt `apps/client/e2e/browser-tutor.mjs`'s launch to open your worktree's export (port 8093), call the worker's `sample` message (or drive one text-fallback Discuss turn) on WebGPU with q8, then with fp32 (make the dtype configurable through the existing `debug` field of the init/download payload, or an env-driven constant in `loadTts`; document which), capture `playPcm` PCM to WAV, and score both with the same round trip. This is the case that matches Noel's machine.
3. Fix what the scores say: if WebGPU q8 scores badly and fp32 (or wasm q8) scores well, change `loadTts`'s device/dtype order accordingly and record the numbers in a comment above it. Independently add `packages/voice/src/browser-cascade/tts-text.ts` + test: `prepareForSpeech(sentence)` that removes any residual markdown/emoji, normalizes quotes, dashes and ellipses to plain punctuation, expands `°`, `%`, `&` to words, and splits any sentence over N characters (pick N from kokoro-js's documented limit, comment the source) at a comma or conjunction into pieces; `speakSentence` calls it and speaks each piece in order under one utterance id.
4. `playPcm`: verify the Int16→float conversion and the 24 kHz buffer on a 48 kHz context by a unit test in `packages/voice/test/web-audio.test.ts` if the existing test harness can reach it; otherwise inspect and state VERIFIED in the report.

Proof: the Node round-trip table in the report; the browser matrix table; `pnpm --filter @sotto/voice test` green with the new tests; a before/after pair of WAVs for the sentence "The dog was a big native husky, the proper wolf-dog, gray-coated and without any visible or temperamental difference from its brother, the wild wolf." at `~/Claude/sotto-run9/C/before.wav` and `after.wav`.

Permissions: owns `tts-text.ts` + test, `loadTts` and `speakSentence` and the `sample` case in `worker.ts`, `models.ts`'s `KOKORO` entry (dtype field only, if you add one), `packages/voice/scripts/tts-roundtrip.mjs`, `web-audio.test.ts`, `planning/run9/C-report.md`. Not `transcribeSegment`, not the prompt, not `llm-turn.ts`, not the client.

Stop when: both tables exist, the fix (if any) is committed with its numbers, tests green, typecheck and lint clean, report committed.

Escalate when: kokoro-js cannot run in Node on this machine after two attempts (then do the browser matrix only), or WebGPU headless fails twice (then ship on the Node table and say the browser case is unverified).
