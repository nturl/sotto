# Lane D — the mic stays usable and the state label tells the truth

Task: on the voice screen, the learner can always speak or interrupt, what the tutor heard is visible and correctable, and the state label matches what the speakers are doing.

Inputs: `apps/client/app/voice/[bookId].tsx`, `apps/client/src/ui/voice/*` (find the mic control, the state label, the caption list, `TextFallback`), `apps/client/src/voice/controller.ts` (+test), `sessionManager.ts` (read only: `speaking` claims the audio bus), `packages/voice/src/events.ts` (`VoiceState`), `packages/voice/src/browser-cascade/provider.ts` (`audio_start`/`audio_end` handling; read only), the screenshot description in `planning/run9/PLAN.md`, `planning/BUGS-TUTOR-RUN5.md` #6, `planning/design/DESIGN.md` (tokens, "no pills", ink-3 rule), `planning/run8/FINAL.md` for the reader's "Talk about this passage" entry.

Output:
1. Confirm or refute (read the code, then a screenshot from your worktree's export on port 8094 at 1440 and 375 in the `speaking` state; the reader route and testIDs are in `apps/client/e2e/voice-live.mjs`): the push-to-talk / open-mic control disappears while the tutor speaks. Whatever the cause, the control renders in every non-error state; pressing it while `speaking` calls `session.interrupt()` first, then starts capture. Hold-to-talk needs to work with mouse, touch, and keyboard (space held) on web.
2. Learner caption: what STT heard shows as the learner bubble as today; add a small "Not what you said? Type it" affordance under the newest learner caption that focuses `TextFallback` with the text prefilled (pure decision in a `.ts` sibling with a test: which caption gets the affordance, and only in the browser cascade / local paths where STT is local, not in own-provider mode if that path already shows something equivalent).
3. State label: `speaking` must persist until playback ends, not until generation ends. Find where the client learns `audio_end` (provider) and whether the store's voice state can be held at `speaking` until the queue drains (`web-audio.ts` knows `playbackQueueEndAt`; the provider can emit `state: listening` only after the last source's `onended`). Do this at the client/provider layer, not in the worker (lane A owns the worker's state posts). Pure logic in a `.ts` sibling with a test.
4. i18n for the "didn't catch that" line: lane A posts a fixed English caption from the worker; add a client-side mapping so a caption whose text equals that constant renders the localized string (key `voice.didNotCatch`), via the i18n script, all nine locales.

Proof: `pnpm --filter @sotto/client test` green with the new tests; screenshots at 375 and 1440 of the voice screen in `listening` and `speaking` with the control visible in both, at `~/Claude/sotto-run9/D/`; `voice-live.mjs` still passes against the main-tree Metro on :8081 if it currently passes (run it once before you change anything and paste both results).

Permissions: owns `apps/client/app/voice/[bookId].tsx`, `apps/client/src/ui/voice/**`, `apps/client/src/voice/controller.ts` + test, new `.ts` siblings under `apps/client/src/voice/`, the nine i18n JSONs only through the script, `planning/run9/D-report.md`. `packages/voice/src/browser-cascade/provider.ts` ONLY for the `speaking`-until-drained change if it cannot be done client-side (say which in the report). Nothing in the worker, prompt, or e2e scripts other than keeping selectors working.

Stop when: tests green, typecheck and lint clean, screenshots taken, report committed.

Escalate when: the hold-to-talk control is generated by a shared component used outside the voice screen and the fix would change other screens (describe the change, do not make it).
